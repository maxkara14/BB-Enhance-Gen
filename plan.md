# Enhance Gen: план развития

Направление одобрено пользователем 2026-09-11. Работа ведётся в ветке
`enhance-test`; проверенные изменения разрешено коммитить и пушить в неё.
Основная ветка не изменяется. Сначала надёжность, затем удобство и качество.

## Этапы

### Director прямо в поле чата

1. **Выполнено** — убрать предпросмотр Director «Мне» и шаг «Далее», сохранять указание и возврат исходного черновика.
2. **Выполнено** — проверить прямую вставку, отмену, ошибки, повторный запуск и сохранность черновика; self-review.
3. **Выполнено** — обновить документацию и подготовить публикацию в `enhance-test`.

Версия 1.3.1. Прошли 10 unit-тестов и 41 браузерный сценарий на desktop/mobile,
проверка синтаксиса и `git diff --check`. Просмотрен снимок меню «Своё».
Director «Мне» вставляет завершённый результат в поле чата; Enhance/Improve сохраняют
предпросмотр. Проверки используют подменённые API, без запросов к реальным моделям.

### Чистая генерация и «Своё → Мне»

1. **Выполнено** — отдельный запрос основной модели, художественный контекст и точная инструкция разыграть указанное действие.
2. **Выполнено** — проверки regex, технических блоков, сохранности текста, отмены и ошибок; self-review.
3. **Выполнено** — документация и подготовка к публикации в `enhance-test`.

Версия 1.3.0. Прошли 10 unit-тестов и 39 браузерных сценариев на desktop/mobile,
проверка синтаксиса и `git diff --check`. Установленные функции raw API проверены
на подменённом транспорте Chat Completion; качество реальных генераций остаётся
ручной проверкой. Полный preset/World Info не включается автоматически;
общие обработчики prompt сторонних расширений остаются активны.

### Первоначальный план

1. **Выполнено — сохранность и управление операциями**
   - Сохранять оригинал; не затирать правки, сделанные во время запроса.
   - Привязывать запросы, окна и применение результата к исходному чату.
   - Отмена, тайм-аут, единая блокировка и защита от повторной отправки.
   - Проверка: поздний ответ не изменяет другой чат или новый черновик.
2. **Выполнено — надёжность ответов**
   - Проверять структуру JSON FT/TS; отклонять неверные типы и поля.
   - Различать полный, оборванный, ограниченный и пустой ответ.
   - Понятные ошибки; полный промпт не попадает в обычные логи.
   - Проверка: ошибочные данные не применяются; штатные ответы работают.
3. **Выполнено — удобство редактирования**
   - Предпросмотр оригинала и результата, применение, повтор, отмена.
   - Возврат оригинала после применения с защитой последующих правок.
   - Степень расширения, сохранение реплик, лицо повествования, язык.
   - Проверка: поток отображается отдельно, исходный текст сохраняется.
4. **Выполнено — качество режиссуры**
   - Глубина и бюджет контекста; очистка служебных подсказок.
   - Явный контекст персонажа, персоны и сцены для Custom API.
   - Интенсивность событий; романтическое, конфликтное, тревожное напряжение.
   - Редактируемые и собственные FT/TS, обновление вариантов, авторский обход запрета.
   - Проверка: итоговая инструкция отражает выбранные настройки и правки.
5. **Выполнено — полировка и итоговая проверка**
   - Ручной вопрос Action Roll без модели, пропуск анимации.
   - История бросков по чатам; старую общую историю сохранить отдельно.
   - Полная RU/EN локализация, Escape, фокус и клавиатурная навигация.
   - Адаптивные окна, уменьшение анимации, документация.
   - Регрессионные и интеграционные проверки, self-review.

## Решения

- Не добавлять runtime-зависимости и не менять устройство SillyTavern.
- Использовать установленный код SillyTavern для проверки интеграционных API.
- Не вызывать реальные модели и не отправлять сообщения в пользовательские чаты
  при автоматических проверках: использовать изолированный стенд.
- Записывать реальные ограничения проверки; не выдавать стенд за проверку
  реального провайдера или пользовательского сервера.

## Проверки и результаты

Завершено 2026-09-11, версия 1.2.0:

- 10 unit-тестов прошли (`node --test tests/core.test.mjs`).
- 22 браузерных сценария прошли в desktop и mobile viewport; ошибок JavaScript нет.
- Проверка синтаксиса `index.js`, `core.js`, `ui.js` и `git diff --check` прошли.
- Скриншоты предпросмотра просмотрены, мобильные действия доступны.
- Self-review: исправлены очистка custom-подсказок, фокус после закрытия окна,
  кнопки внутри формы, восстановление после отказа отправки и тайм-аут native API.
- Браузерный стенд загружает реальные ES-модули расширения в Chrome,
  подменяя API/чат; реальные провайдеры и пользовательские чаты не вызывались.

Ограничения: основная модель возвращает только текст, без метаданных завершения;
обнаружение `finish_reason=length` доступно для Custom API. Проверка качества
генераций, групповых чатов и сочетаний сторонних тем/расширений остаётся ручной.
Подробнее: [docs/playbooks.md](docs/playbooks.md).

Публикация проверенной реализации разрешена в `origin/enhance-test`.
Слияние в `main` в этот план не входит.

## Исправление поля «Своё» и пояснения режиссуры

1. **Выполнено** — убрать переполнение поля в боковом меню и пояснить область действия и значения настроек режиссуры.
2. **Выполнено** — проверить ввод, переходы и геометрию на desktop/mobile, проверить пояснения и выполнить self-review.
3. **Выполнено** — записать результат и подготовить проверенные изменения к публикации в `enhance-test`.

Результат 1.2.2: поле «Своё» помещается в боковое меню без горизонтальной прокрутки;
пояснения интенсивности и типа напряжения показывают область действия и меняются при выборе значения.
Проверено: 10 unit-тестов, 33 браузерных сценария на desktop/mobile, синтаксис, `git diff --check`.
Просмотрены снимки поля и пояснений. Браузерный стенд использует подменённые API и чат.

## Корректировка по отзывам: настройки, меню и профили

Направление задано пользователем: оформление Comic Forge / Visual Novel Engine,
совместимость с Extension-Sorter, возврат бокового меню событий и профили подключения.

1. **Выполнено** — вернуть стандартную оболочку `inline-drawer`, оформить компактные
   секции настроек, сохранить положение внутри папки Sorter при смене языка.
2. **Выполнено** — восстановить плавное боковое подменю; добавить источник
   «Профиль SillyTavern» с отменой/тайм-аутом без смены активного подключения.
3. **Выполнено** — проверки сортировки, профилей и геометрии меню, визуальная проверка,
   документация и self-review. Изменения подготовлены к публикации в `enhance-test`.

Результат 1.2.1: стандартная оболочка и стабильный узел настроек для Sorter,
компактные секции, отдельное боковое подменю с учётом границ экрана, три источника
генерации (основное подключение, профиль, Custom API). Старый выбор Custom API
сохраняется при обновлении. Ответы «Боту» используют основное подключение.

Проверки: 10 unit-тестов, 31 браузерный сценарий на desktop/mobile, синтаксис и
`git diff --check`. Просмотрены снимки настроек и бокового меню. Проверка Sorter
использует его установленные функции распознавания/идентификации, а Connection
Manager в стенде подменён: реальные профили к провайдерам не обращались.
Профили возвращают готовый текст без стриминга; отмена и тайм-аут поддерживаются.
